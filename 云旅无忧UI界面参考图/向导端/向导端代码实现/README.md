# 云旅无忧向导端 UI 实现

这是根据当前文件夹内 46 张 UI 参考图实现的静态前端原型。

## 文件

- `index.html`：页面入口
- `styles.css`：视觉样式与移动端布局
- `app.js`：46 个界面、路由切换与组件数据

## 预览

直接用浏览器打开 `index.html` 即可预览。页面左侧为界面列表，点击可切换对应参考图页面；也可以使用键盘左右方向键切换。
